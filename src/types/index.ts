export interface HeroFeature {
  icon: string;
  title: string;
}

export interface ClientLogo {
  src: string;
  alt: string;
}

export interface WhyStudyBox {
  icon: string;
  title: string;
  text: string;
}

export interface StudyProgramBox {
  plusColor: string;
  title: string;
  text: string;
}

export interface CourseCard {
  icon: string;
  title: string;
  text: string;
  btnText: string;
}

export interface TestimonialItem {
  image: string;
  name: string;
  role: string;
  company: string;
  quote: string;
  youtubeUrl: string;
}
